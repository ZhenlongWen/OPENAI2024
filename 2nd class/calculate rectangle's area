const length = parseFloat(prompt("Enter the length of a rectangle:"));
const width = parseFloat(prompt("Enter the width of a rectangle:"));
var area = length * width
console.log(`The area of the rectangle is: ${area} square units.`);
