
    const firstName = prompt("Enter your first name:");

    const lastName = prompt("Enter your last name:");

    const randomNumber = Math.floor(Math.random() * 2);

    if (randomNumber === 0) {
        alert("Random name: " + firstName);
    } else {
        alert("Random name: " + lastName);
    }
